# Calgary Condo Leads 1.0.22 plan

The Southeast Calgary tab is still resolving through the legacy query URL and old hero. Fix should remove query-param navigation from homepage tabs and redirect legacy query URLs to clean hash routes.
